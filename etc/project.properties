version=1.1
build.dir=build
etc.dir=etc
lib.dir=lib
license.dir=license
src.dir=src

axis.gen.dir=${build.dir}/gen-axis

client.gen.dir=${build.dir}/gen-client

dist.zip=${build.dir}/zip
dist.dir=${build.dir}/dist
dist.bin.dir=${dist.dir}/bin
dist.ant.dir=${dist.bin.dir}/ant
dist.doc.dir=${dist.dir}/doc
dist.javadoc.dir=${dist.doc.dir}/api
dist.license.dir=${dist.dir}/license
dist.etc.dir=${dist.dir}/etc
dist.jar.dir=${dist.dir}/jar
dist.lib.dir=${dist.dir}/lib
dist.src.dir=${dist.dir}/src
dist.java.src.dir=${dist.src.dir}/java
dist.jibx.src.dir=${dist.src.dir}/jibx
dist.axis.gen.dir=${dist.src.dir}/gen-axis
dist.client.gen.dir=${dist.src.dir}/gen-client
dist.junit.src.dir=${dist.src.dir}/test

doc.dir=${build.dir}/doc
javadoc.dir=${doc.dir}/api
javadoc.title="eSciDocCore infrastructure client project"
docbook.dir=${doc.dir}/docbook
pdf.dir=${doc.dir}/pdf
docbook.api.chapter.title=Api Reference for eSciDocCore Infrastructure Service Clients and Resources

lib.doc.dir=${lib.dir}/doc

lib.jibx.dir=${lib.dir}/jibx-1.2.1
jibx.src.dir=${src.dir}/jibx
jibx.binding.base.dir=${jibx.src.dir}/binding
jibx.soap.resource.binding.file=${jibx.binding.base.dir}/soap/resource-binding.xml

dbdoclet.document=api.xml
docbook.gen.dir=${build.dir}/gen-docbook


java.src.dir=${src.dir}/java
java.build.dir=${build.dir}/classes

junit.src.dir=${src.dir}/test

src.bin.dir=${src.dir}/bin
src.dist.dir=${src.dir}/dist
src.dist.bin.dir=${src.dist.dir}/bin

src.doc.dir=${src.dir}/doc
src.docbook.dir=${src.doc.dir}/docbook
